# Poetry configuration update for new dependencies
# Add these to pyproject.toml [tool.poetry.dependencies]:

additional_deps = """
structlog = "^23.3.0"
python-multipart = "^0.0.6"
email-validator = "^2.1.0"
"""

# Run: poetry add structlog python-multipart email-validator
